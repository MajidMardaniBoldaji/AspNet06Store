@model List<Product>

<h1>Welcome to Asp.netCore 6</h1>
<h2>Majid Mard</h2>

@foreach (var item in Model)
{
 
    <div>
        <h4>@item.Name<span><small>@item.Price</small></span></h4>
        <p>@item.Description</p>
    </div>
}